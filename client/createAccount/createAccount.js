const { isEmptyObject } = require("jquery");

function saveData() {
    let data = document.getElementsByClassName(input-text).value;
    DataTransfer(email, password) to URL("C:/Users/Alexandru/Desktop/IuliaClothesShop/server/db");
    if isEmptyObject(obj: "input-text") = alert("Complete every item!");
}